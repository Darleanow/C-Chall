#include <stdio.h>
#include <math.h>

void main()
{
    const float PI = 3.14159;
    const char c = ':';
    int r = 20;
    float area = PI*r*r;
    char v = c;
    printf("The area of the circle %f", area);
    getchar();
}